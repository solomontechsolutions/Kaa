import { LandlordSignInForm } from "@/components/landlord/sign-in-form";

export const metadata = { title: "Sign in", robots: { index: false, follow: false } };

export default function LandlordSignIn() {
  return <LandlordSignInForm />;
}
